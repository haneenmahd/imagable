import styled from "styled-components";

const FooterStyle = styled.footer`
  display: flex;
  flex-direction: row;
  align-items: flex-start;
  padding: 76px 63px;
  background: linear-gradient(180deg, rgba(32, 32, 32, 0.94) 0%, #202020 100%);
  border-radius: 40px;
  width: 90vw;
  margin: 40px calc(100vw / 20);
`;

const Footer = () => {
    return (
        <FooterStyle>

        </FooterStyle>
    );
}

export default Footer;